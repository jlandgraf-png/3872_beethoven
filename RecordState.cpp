#include <iostream>
#include <vector>

while(1){
    uLCD_main.print("INPUT NOTE USING DIAL");
    vector<double> freqVector;
    if (STORE()){
        freqVector.pushback(readFrequencyInput());
    }
}